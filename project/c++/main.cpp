#include <iostream>
#include <ctype.h> 
#include <bits/stdc++.h>
#include <vector>
#include <typeinfo> 
#include <math.h> 
#include <string>
#include <queue> 
void myf(int listName[]){
	std::queue<int> listName;
	char name[];
	int lengthArray = sizeof(listName) / sizeof(int);
	for(int i=i;i<lengthArray;i++ ){
		name.
	}
}
